# InsectWorld AI 与工程规范

本文件适用于整个仓库。子目录若有更具体的 `AGENTS.md`，只可补充局部约束，不得放宽本文件的 MUST 条款。

## 1. 开始任务前（MUST）

1. 阅读根目录 `README.md`、`docs/README.md`、相关产品边界、ADR 和目标目录文档。
2. 优先使用 codebase-memory MCP 的知识图谱发现代码：`search_graph` → `trace_path` → `get_code_snippet` → `query_graph` → `get_architecture`。工具不可用、结果不足或需要搜索字面量/配置时才使用 `rg`。
3. 执行 `git status --short`，识别并保护用户已有改动。禁止回退、覆盖、移动或批量格式化无关改动。
4. 明确任务属于通用内核、SLG 领域模块、游戏包还是题材扩展，并确认数据所有者和事务边界。
5. 历史 `.codeartsdoer/specs/` 和日期报告只提供实施背景；现行产品边界、已接受 ADR、代码和测试证据优先。

## 2. 产品与架构边界（MUST）

项目采用四层能力模型：

| 层级 | 允许内容 | 禁止内容 |
|---|---|---|
| 通用内核 | 配置版本、事件、公式、规则注册、幂等、可观测性等稳定技术能力 | SLG 或昆虫题材知识 |
| SLG 领域模块 | 地图、行军、战斗、经济、联盟、赛季等可选机制 | 具体种族、数值和游戏包依赖 |
| 游戏包 | 内容、数值、模块装配和既有规则组合 | 修改通用内核来迁就单个题材 |
| 题材扩展 | 配置无法安全表达的专属行为 | 绕过公开契约直接访问其他模块内部 |

- MUST：领域模块不得 import 游戏包或题材扩展。
- MUST：新增通用抽象至少有两个真实用例，或是解决当前明确的跨层依赖。
- MUST：配置只表达数据与受控规则组合，不把任意业务代码塞进表达式。
- SHOULD：首个可玩闭环完成前，不新增微服务；先保持逻辑边界清晰。
- SHOULD：不实现规格未要求、无调用方或无法写出验收用例的功能。

产品边界以 `docs/product/产品与平台边界.md` 为准，目标架构以 `docs/architecture/目标架构.md` 和已接受 ADR 为准。

## 3. DDD 与依赖方向（MUST）

每个限界上下文遵循：

```text
interfaces → application → domain ← infrastructure
```

- `domain` 不 import infrastructure、传输层、ORM、数据库或消息队列 SDK。
- `application` 只通过领域接口使用仓储、事件和外部能力，不直接 import infrastructure。
- Repository 接口放在 domain，实现放在 infrastructure，在启动入口注入。
- 聚合根内强一致；跨聚合通过 application 编排或领域事件协作，禁止聚合对象直接互调。
- 跨上下文写入默认使用本地事务 + Outbox + 幂等消费者；确需同步调用时必须说明失败与超时语义。
- CQRS 只在读写模型确实不同或读侧需要独立投影时使用，不机械创建空目录。
- 一个业务表只有一个上下文拥有写权限；其他上下文通过契约或投影访问。

## 4. Go 代码规范（MUST）

- 所有 Go 代码通过 `gofmt`；import 通过 `goimports` 或等价工具整理。
- 包名小写且职责单一，禁止新增 `utils`、`common`、`helpers` 大杂烩包。
- 导出标识符使用 PascalCase，未导出标识符使用 camelCase，receiver 使用类型首字母小写。
- 可阻塞或跨边界函数的第一个参数是 `context.Context`。
- 所有错误必须检查；向上传递时用 `%w` 包装业务上下文。业务错误使用所属上下文的错误码，不裸散落 `errors.New`。
- 业务失败返回 error，不 panic。goroutine 必须有 context、关闭信号或可证明的生命周期管理。
- 优先强类型；除异构注册表等必要场景外，不使用 `any`/`interface{}` 作为通用容器。
- 接口只为多个实现、跨层反转或测试替身建立；Repository 接口除外。
- 不提交未使用代码、注释掉的旧实现或无调用方的预留扩展。

## 5. 常量与类型（MUST）

- 常量就近归属：聚合常量放对应包，跨聚合常量放所属 domain，扩展点 ID 集中在配置注册表，错误码集中在上下文错误包。
- 业务分支中禁止魔法数字；框架默认超时等局部常量必须命名并说明依据。
- ID、资源数量、积分和金额使用 `int64`；金额单位为分。
- 状态和枚举使用具名整型类型与常量；协议层使用 `int32`。
- 持久化与传输时间戳使用 Unix 毫秒 `int64`；领域内可使用 `time.Time`。
- 格子坐标使用整数；离散业务量禁止使用浮点。
- `float64` 仅用于确有小数需求的配置系数和中间计算，最终业务量需明确舍入后转为整数。
- 具体数值优先来自游戏包配置；通用代码不得含昆虫题材名、种族名或游戏平衡数值。

## 6. 注释与文案（MUST）

- 所有手写 Go、Proto 和 DDL 注释使用简体中文；标准技术缩写和生成代码除外。
- 包必须有 `// Package <name> ...` 中文包注释。
- 导出标识符必须有以标识符名开头的中文注释，重点解释业务意图与约束。
- 所有 struct 字段必须有中文注释，说明业务含义、单位、来源或范围；整数枚举字段列出全部取值含义。
- TODO/FIXME 使用中文并说明触发条件或后续动作。
- 客户端可见错误文案用中文，稳定错误码用英文标识。
- 注释不得伪造完成状态，不复述显而易见的代码。

## 7. 数据与协议（MUST）

- MySQL 表名使用 `t_` + 单数 snake_case；关联表为 `t_<a>_<b>_rel`；分片表后缀使用两位编号。
- 普通索引 `idx_<entity>_<field>`，唯一索引 `uk_<entity>_<field>`，外键 `fk_<entity>_<field>`。
- SQL/ORM 表名通过所属 persistence 包常量引用；禁止字符串拼接 SQL，必须参数化。
- Schema、事件和公开 API 变更必须声明兼容策略。Proto 字段号不得复用，删除字段需 reserved。
- 外部写命令必须有幂等键；事件必须有稳定事件 ID、事件类型、版本、发生时间和追踪字段。
- 运行中实例绑定配置版本；删除或回滚配置前必须检查活动引用。
- 数据库迁移应可前向兼容滚动发布；破坏性迁移拆成扩展、回填、切换、清理多个阶段。

## 8. 日志、指标与安全（MUST）

- 使用 zap 结构化日志，业务代码禁止 `fmt.Println` 和 `log.Printf`。
- 关键命令、聚合状态变更、事件投递/消费、跨边界调用、配置发布、审计操作和异常分支必须记录日志。
- 业务日志至少包含 `trace_id` 或 `request_id`、操作、对象 ID、结果和必要上下文；涉及玩家时包含 `player_id`。
- 记录耗时使用字段，不把上下文拼进长字符串。
- Token、密码、支付信息、个人信息和完整配置内容不得明文入日志；审计日志与普通业务日志分离。
- 可恢复异常用 Warn/Error，编程错误才可 DPanic/Panic；禁止用日志代替错误处理。
- 新核心链路同时定义成功率、延迟、积压或一致性指标，告警阈值必须有 SLO 或压测依据。

## 9. 测试与验证（MUST）

### 本地 MySQL 开发基线

- 本机集成测试默认连接 `127.0.0.1:3306`，账号 `root`，密码 `123456`，仅允许操作 `insect_world_test`。
- 该账号只用于当前个人开发环境，禁止复制到共享、测试或生产部署配置，禁止写入业务日志。
- 重建脚本只能删除以 `_test` 结尾的数据库；执行真实 MySQL 仓储测试前先运行 `scripts/reset_local_mysql.sh`。
- 自动化环境可通过 `MYSQL_HOST`、`MYSQL_PORT`、`MYSQL_USER`、`MYSQL_PASSWORD`、`MYSQL_TEST_DATABASE` 覆盖本地默认值。

### 测试层次

- domain：状态机、不变量、边界值、确定性和并发规则；
- application：用 mock/内存替身验证编排、权限、幂等与失败补偿；
- infrastructure：使用真实或容器化依赖验证 SQL、序列化、事务和适配；
- interfaces：协议映射、鉴权、校验、错误码和关键兼容路径；
- integration/E2E：验证跨上下文主链路、重复投递、恢复和配置版本边界。

### 强制规则

- 修改行为必须新增或更新能够先失败后通过的测试。
- 表驱动测试和 `t.Run` 优先；断言必须验证业务结果和关键副作用。
- 并发相关代码执行 `go test -race`。
- 公式和战斗必须验证相同输入、随机种子、配置版本得到相同结果。
- 幂等场景至少验证重复命令、重复事件和超时重试。
- 不用无断言、只覆盖 getter 或照抄实现的测试追求覆盖率。

覆盖率是风险信号而非单一完成标准。新增核心 domain 代码 SHOULD 达到 90%，application SHOULD 达到 80%，但关键不变量、失败分支和接口安全路径必须 100% 有明确用例。低于目标需在交付说明中解释风险和后续。

### 验证顺序

1. 修改包单元测试；
2. 所属 module：`go test -race ./...`；
3. `scripts/ci_check.sh` 或等价静态检查；
4. 受影响的集成/E2E；
5. 高风险或跨共享模块改动执行工作区全量测试。

不得把未执行的验证写成已通过。失败必须区分本次引入、既有失败和环境缺失。

## 10. 文档与 ADR（MUST）

- 文档权威层级和状态规则见 `docs/README.md`。
- 新服务/部署单元、跨上下文数据所有权、公开协议不兼容变更、核心技术选型和通用层边界变化必须写 ADR。
- 普通实现细节写在代码、测试或对应领域文档，不为每个任务新增“总设计”。
- 一个概念只维护一个权威定义，其他位置使用相对链接。
- 易变化的模块数、服务数、配置数和覆盖率不得散落在多份文档。
- 实现完成后只更新实际变化的文档；历史报告不回写成现行规范。

## 11. 交付要求（MUST）

每次交付说明包含：

1. 已实现的可观察结果；
2. 关键边界或设计取舍；
3. 主要修改和删除文件；
4. 实际验证命令及结果；
5. 遗留风险、既有失败和明确后续。

代码审查依次检查：需求行为、数据所有权、依赖方向、并发与幂等、兼容性、安全与日志、测试质量、文档一致性。违反 MUST 条款不得合入。
