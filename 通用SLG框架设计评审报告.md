# 通用SLG框架设计评审报告

> 评审对象：《通用SLG框架设计书.md》+ docs/engine 九大板块 + docs/game 昆虫世界设计 + server/ 实现现状（212个Go文件，DDD四层）
> 评审诉求：目标做一款SLG游戏，同时构建"换玩法只需改配置+替换UI"的通用SLG底层服务，验证当前设计是否成立
> 评审方式：设计域（玩法通用性）× 工程域（技术架构）双视角独立评审后汇编
> 评审日期：2026-08-10
> 质量门判定：**CONCERNS**（方向成立，但有 3 个高风险项必须在进入全量制作前解决）

---

## 一、总体判断

**架构方向成立，核心理念（骨肉分离、契约驱动、零硬编码）是专业的，文档完成度在同类项目中属于上游水平。** 但评审的最终结论是：**"换玩法只改配置+换UI"这个核心承诺，在当前的契约体系下只对"同范式换皮"成立，对"真正换玩法"不成立**。这不是理念错误，而是表达力（Expressive Power）不足导致的承诺过度。

两个评审视角独立得出同一组结论（互不知晓，结论收敛，可信度高）：

| 收敛点 | 设计域（文策渊） | 工程域（程基岩） |
|--------|----------------|----------------|
| 换皮承诺过度 | ✅ 只对同范式换皮成立 | ✅ "换UI即换游戏"过度承诺 |
| 公式/规则引擎表达力不足 | ✅ 7种动作+简单比较撑不起复杂机制 | ✅ 四则运算公式引擎撑不起真实SLG公式 |
| 需要先做垂直切片验证 | ✅ 三题材验证≠契约表达力验证 | ✅ 先垂直切片再决定模块拆分 |
| 配置热更边界风险 | —（隐含） | ✅ 热更×快照×权威方三方冲突 |

---

## 二、核心问题清单（按严重级别）

### 【高】P1：规则系统/公式引擎的表达力不足以支撑"换玩法"

- **依据**：设计书 2.7 节动作类型固定 7 种（apply_buff / remove_buff / modify_resource / spawn_entity / trigger_combat / send_notify / change_terrain）；条件表达式仅支持简单比较（>/</==/in/and/or）。公式引擎仅"四则运算+变量替换"。
- **为什么是问题**：三国SLG的"策反""占领""附庸进贡"、星际SLG的"跃迁""层间流转"，需要条件分支、随机、循环、函数调用。7种动作+四则运算无法表达这些机制——它们要么被"降级表达"成四不像，要么必须扩展框架代码（扩展接口.md 中三个示例场景全部标注"✅需要修改框架代码"）。
- **自证矛盾**：设计书宣称"换皮=换肉，骨头不动"，但扩展指南中每个真实差异化玩法都需要动骨头。三题材验证是**文档映射级**验证（玩法特征→模块→配置逐行映射），证明的是"模块覆盖"，不是"契约表达力"。100% 评分有方法论瑕疵。
- **修复建议**：
  1. 公式引擎升级为 AST 表达式 + 函数库（min/max/floor/ceil/random/clamp）+ **种子化确定性随机**（战斗回放要求）；
  2. 规则系统动作类型增加可扩展注册机制（注册表驱动，而非写死枚举），让"新增动作类型=注册新handler"而非"改框架核心"；
  3. 新增"契约表达力缺口对照表"：每个题材的每个玩法特征 × 所需契约能力 × 当前缺口 × 修复归属（配置/新扩展点/新动作handler）。

### 【高】P2：配置热更 × 战斗快照 × 权威方交接三方冲突

- **依据**：spec.md 定义热更时"战斗用开战时配置快照，移动/生产下个Tick用新配置"；同时 combat 战斗中属性每轮从 Social 刷新、公式/克制矩阵用开战快照。
- **为什么是问题**：热更删除某兵种后——Social 已按新配置回写属性，Combat 仍按旧公式/旧克制计算，形成**隐性引用断裂**；运行中的战斗可能引用已删除的配置项，而"配置包兼容性校验"只覆盖存量数据（玩家资源/地形/兵种），**不覆盖进行中的战斗实例**。回滚机制只定义了"校验失败回滚"，未定义"已应用新配置后回滚"时进行中业务的配置版本归属。
- **修复建议**：
  1. 战斗快照绑定 `config_version`，结算时校验快照配置仍存在，缺失则走熔断协议（用兜底配置结算或强制平局）；
  2. 回滚定义双向切换语义：新配置→旧配置时，进行中业务如何归属（继续用新/回退旧）；
  3. 热更删除配置项时，必须扫描进行中实例引用（战斗、行军、生产队列），阻塞或降级。

### 【高】P3：工程节奏风险——"先重构后功能"使全部服务共享同一失败面

- **依据**：tasks.md 显示 P0 任务组 0/0b/0c/0d/0e/0f 共 6 组纯重构（目录重构→server/→8 module→共享内核→统一表定义→Persist 建立），212 个文件经历两次 import 迁移后才开始产生业务产出；当前实现进度 0/完成。
- **为什么是问题**：微服务拆分 + 强一致性链（战斗→资源→积分→赛季）全走最终一致+事件，权威方交接存在崩溃窗口；若设计错误，返工面是全部 8 个服务。8 微服务 + NATS + etcd + Redis + MySQL + MongoDB 对 SLG 首款产品而言基础设施过重（YAGNI 违规）。
- **修复建议**：
  1. **先做垂直切片**：单 module（或最小 3 服务：战斗/经济社交/运营配置）跑通"战斗→结算→资源变更→积分→赛季"主链路，验证三个高风险（公式引擎、热更边界、强一致链）可行性后再决定模块拆分；
  2. 重构与功能并行，避免 6 组 P0 纯重构空转；
  3. 写路径：Redis/本地 WAL 写主 + MySQL 异步落库，避免聚合根+Outbox 同事务落库在请求热路径上（P99<100ms 难达标）。

### 【中】P4：战斗系统只覆盖"自动演算型"，不覆盖实时/战棋/舰队战

- **依据**：设计书 2.4 节战斗为 tick 制自动演算；docs/engine/03-业务架构/03-战斗系统.md 未定义实时战斗（RoK 式地图战斗）、回合战棋、舰队战的通用状态机。
- **为什么是问题**：真实 SLG 战斗差异极大（COK 式即时、RoK 式地图战斗、三国式回合、星际式舰队），"伤害公式配置+tick演算"只覆盖其中一类；三题材验证中"太空战/轨道战/地面战"被映射为"三种 combat_types"，但未证明同一战斗状态机能承载三种战斗范式。
- **修复建议**：明确定义战斗系统支持范式边界（自动演算型为主），把"实时战斗/战棋"列为需要新增扩展点（combat_engine 替换点）的场景，写入能力边界文档，避免承诺不存在的通用性。

### 【中】P5：客户端"换UI"被严重低估

- **依据**：spec.md 3.2 外部系统仅定义客户端通过 WebSocket 交互；docs/engine/08-客户端契约/ 只有同步契约和输入协议。
- **为什么是问题**：换题材时客户端除 UI 外还要改表现层（地图渲染、战斗演出、特效、动画）、本地化、新手引导、**公式镜像**（客户端预测需要镜像服务端公式）、输入协议扩展。仅"换UI"的承诺会误导换皮成本评估。
- **修复建议**：把"换皮"对外明确定义为"换内容配置 + 换表现层 + 换引导/本地化"三件套；服务端 Command/Push 消息用注册表驱动（新增玩法=注册新 command 类型），避免每次新增玩法改 proto + 分发代码。

### 【低】P6：其他技术债务

| 项 | 问题 | 建议 |
|----|------|------|
| etcd watch 断连 | 服务重启/抖动错过的 revision 未定义重放，config.reloaded 可能漏报 | 按 last revision 重放 |
| wallet.go 精度 | 用 float64 修正器乘 int64 后截断，资源精度风险，与 99.999% 准确率冲突 | 整数 bps/定点数 |
| 持久化一致性 | 战斗/交易/掠夺实时持久化的性能与强一致冲突 | 明确 WAL→DB 两阶段写协议 |

---

## 三、设计亮点（应保留，勿因评审动摇）

1. **骨肉分离与契约格式**：挂载点ID + 输入/输出契约 + 校验规则 + 默认值 + 示例的六要素契约格式，是专业级设计，直接可落地为 JSON Schema。
2. **配置加载体系完整**：配置包完整性校验 + 兼容性校验 + 拓扑排序 + 热更回滚，是同类项目少有的严谨度。
3. **零硬编码工程纪律**：5 个 CI 扫描工具（zero_hardcode / table_name / ddd_dependency / reskin_validator / spec_scanner）+ AGENTS.md 9 条规范，工程纪律到位。
4. **事件驱动骨架清晰**：Outbox + 事件总线 + 幂等消费 + 权威方交接，可靠性设计有章法。
5. **换皮校验工具已存在**：tools/reskin_validator 说明换皮验证已开始工具化。

---

## 四、建议的下一步（评审门：CONCERNS → 需整改后复审）

1. **（P0，1-2周）做昆虫世界垂直切片**：单 module 跑通"建造→产兵→行军→战斗→结算→资源变更→赛季积分"主链路，用真实代码验证三个高风险项（公式引擎表达力、热更边界、强一致链），产出切片验证报告。
2. **（P0，并行）公式引擎预研**：明确 AST + 函数库 + 种子化随机方案，这是当前最大的知识缺口。
3. **（P1）产出"契约表达力缺口对照表"**：由设计侧逐题材逐契约梳理，作为修复与新增扩展点的依据。
4. **（P1）修改对外承诺**：把"换玩法只需改配置+换UI"修正为"同范式换皮=改配置+换表现层；跨范式换玩法=配置包+扩展点演进"，写回设计书能力边界。
5. **（P2）收敛基础设施**：首款产品用 3 服务或单进程 DDD 模块化起步，Persist 降级为库，按真实瓶颈再拆微服务。

---

## 五、评审参与

- 评审主持：游承峰（游戏开发工作室主理人）
- 玩法通用性评审：文策渊（design-strategist）
- 技术架构评审：程基岩（engineering-lead）
- 一致性检查：两位成员独立评审结论收敛（换皮承诺、表达力、垂直切片、热更边界四点完全一致），本报告结论可信度高。
