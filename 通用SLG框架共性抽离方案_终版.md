# 通用SLG框架 · 共性机制抽离方案（终版）

> 目标定义（2026-08-10用户确认）：**通用 = SLG品类共性机制**；**特殊业务玩法 = 另行扩展开发**；**目标 = 基础打牢、拓展改动方便**。
>
> 2026-08-11 用户追加确认：① 邮件后置（先扩展，跑通后评估）② 任务/成就走配置化（规则系统复用）③ 聊天留拓展（不进框架、补接入规范）。
>
> 分析基础：设计域（文策渊）共性系统拆解 + 工程域（程基岩）扩展机制设计，两人独立结论收敛。

---

## 一、四层分类模型

| 层级 | 定义 | 判断标准 | 代码归属 |
|------|------|---------|---------|
| **A类：通用框架** | 几乎所有SLG都有的机制，代码零题材字符串 | ≥2题材验证 + 机制稳定 + 依赖只向下 | `server/framework/`（L1） |
| **B类：配置化** | 机制通用但内容差异大，纯数据+规则组合可表达 | 已有扩展点+handler能覆盖 | 18个配置文件（L2） |
| **C类：扩展开发** | 单个题材特有或技术侵入深 | 需新行为/新动作类型 | `server/extensions/<题材>/`（L3） |
| **D类：后置** | SLG标配但非第一轮必需品，先扩展开发跑通再评估 | 跑通一个题材后再决定是否提升 | 先放C类，后评估进A类 |

**判定顺序**：先试"规则+事件"→再试配置→题材独立则扩展→≥2题材需要且机制稳定才进框架。

---

## 二、完整抽离清单

### A类【进通用框架 —— 12根骨头 + 9根新骨头】

#### 已覆盖（现有12根骨头，无需调整）

| # | 骨头 | 覆盖状态 |
|---|------|---------|
| 1 | 实体系统 | ✅ 完整 |
| 2 | 地图系统 | ✅ 完整 |
| 3 | 移动系统 | ✅ 完整 |
| 4 | 战斗系统 | ✅ 完整 |
| 5 | 经济系统 | ✅ 完整 |
| 6 | Buff系统 | ✅ 完整 |
| 7 | 规则系统 | ⚠️ 需升级（补行为注册表，见第五章） |
| 8 | 联盟系统 | ✅ 完整 |
| 9 | 赛季系统 | ✅ 完整 |
| 10 | 事件系统 | ✅ 完整 |
| 11 | 网络同步 | ✅ 完整 |
| 12 | 持久化 | ✅ 完整 |

#### 新增（9根新骨头，按优先级排序）

| # | 新骨头 | 共性理由 | 优先级 |
|---|--------|---------|--------|
| 13 | **玩家档案** | 注册/登录/资料/等级/经验/昵称，一切系统的owner基础。user_auth spec已存在可复用 | P0-1 |
| 14 | **建筑升级** | 建造/升级状态机、升级队列、前置依赖、打断/加速。每个SLG的主城/基地都是建筑。用户明确举例 | P0-2 |
| 15 | **采集调度** | 采集队分配/采集状态机（去程→采集中→返程）/召回/地块被占异常。用户明确举例 | P0-3 |
| 16 | **背包/道具** | 道具定义/堆叠/使用/过期。是邮件附件/商店商品/活动奖励的**公共底层**，必须最先定item契约 | P0-4 |
| 17 | **兵种训练** | 训练队列/槽位/加速/取消。SLG核心循环 | P0-5 |
| 18 | **个人科技** | 科技树/研究队列/解锁条件。与联盟科技互补 | P0-6 |
| 19 | **商店骨架** | 商品/货币/购买/限购。机制通用，商品内容配置化。商业化标配 | P1-7 |
| 20 | **排行榜骨架** | 排名计算/更新触发/分页查询。维度（战力/联盟/赛季）配置化。跨玩家查询需骨架约束性能 | P1-8 |
| 21 | **新手引导** | 引导步骤条件/完成标记/跳步。所有系统UI需挂引导点，架构早定便宜 | P1-9 |

**不进框架的SLG高频系统：**

| 系统 | 归属 | 理由 |
|------|------|------|
| 任务/成就 | **B类配置化** | 规则系统实例化（触发事件 → 条件检查 → 奖励动作），复用现有骨头，不新增 |
| 聊天 | **C类扩展** | 机制通用但客户端差异大、合规/敏感词侵入深，常见接第三方。补"聊天接入规范" + 事件总线挂载点 |
| 邮件 | **D类后置** | SLG标配，先放扩展开发。跑通昆虫世界后，评估系统邮件/战报邮件/奖励发放的共性度，再决定是否提升 |

### B类【配置化 —— 18个配置文件覆盖】

战斗公式、克制矩阵、经济数值、产出公式、外交状态机、**任务/成就（规则系统配置化）**。

### C类【扩展开发 —— 题材专属】

昆虫信息素路径/进化树/蛰伏、星际跃迁/三层战斗/虫洞、三国计策/附庸进贡、PVE副本形态、**聊天**（补接入规范）。

### D类【后置 —— 先扩展跑通再评估】

**邮件** —— 如跑通后发现系统邮件/战报/奖励发放机制100%通用，再提升为第22根轻骨头。

---

## 三、优先级排序

### 第一轮「打牢基础」（P0，先补齐最大缺口）

| 优先级 | 系统 | 理由 | 产出 |
|--------|------|------|------|
| P0-1 | **玩家档案** | 0覆盖的最大缺口，一切系统的owner。无此则建筑/背包/科技都无归属 | player实体契约+user-auth接入 |
| P0-2 | **建筑升级** | 用户明确举例，肉已有（buildings.json）骨头缺 | 升级状态机+队列+前置依赖契约 |
| P0-3 | **采集调度** | 用户明确举例，数据面已有骨头流程面缺 | 采集状态机+多队分配+异常处理 |
| P0-4 | **背包/道具** | 公共底层——商店商品/任务奖励/邮件附件全依赖item契约 | item实体类型+道具契约 |
| P0-5 | **兵种训练** | SLG核心循环，与战斗/经济直接耦合 | 训练队列+加速契约 |
| P0-6 | **个人科技** | 与联盟科技互补，主城成长线 | 研究队列+解锁条件契约 |

### 第二轮「补齐运营设施」（P1）

| 优先级 | 系统 | 理由 |
|--------|------|------|
| P1-7 | 商店骨架 | 商业化标配，商品配置化 |
| P1-8 | 排行榜骨架 | 性能敏感（跨玩家查询），需骨架约束 |
| P1-9 | 新手引导 | UI引导点挂载，架构早定便宜 |
| P1 | 任务/成就（B类配置化） | 规则系统实例化，复用骨头零成本 |

### 第三轮「后置评估」（P2）

| 优先级 | 系统 | 理由 |
|--------|------|------|
| P2 | 邮件 | 先扩展开发跑通昆虫世界，验证共性后决定提升 |

---

## 四、分层架构模型

```
L0 基础设施层（稳定、零玩法）
   事件总线 / 配置加载 / 持久化骨架 / 网络同步 / ECS实体系统
   → server/shared/pkg/{eventbus, config, entity, persist}

L1 SLG共性机制层（21根骨头，代码零题材字符串）
   12根老骨头 + 9根新骨头 + 行为注册表（action_registry）
   → server/framework/ 独立 module

L2 题材内容层（肉，18个配置文件）
   种族/兵种/建筑/英雄/科技/资源/地形/克制/公式/Buff/规则/事件/赛季/联盟/移动/战斗/经济/同步/持久化
   → 经 L1 扩展点契约校验注入，各服务经 ConfigQueryAPI 查询

L3 题材特殊扩展层（特殊业务玩法）
   昆虫/三国/星际 各题材独立 module
   → server/extensions/<题材>/ 独立 go.mod，依赖 L0+L1

依赖方向：L0 ← L1 ← L2 ← L3（单向，L1禁止import任何服务或题材代码）
```

## 五、工程落地方案

### 5.1 核心改造：行为注册表（让"拓展改动方便"）

**现状**：`ExtensionRegistry` 是数据注册表（存配置），缺"行为注册表"。规则系统动作类型写死7种枚举，新增动作 = 改框架代码。

**改造路径（3步，不破环现有架构）**：

1. **Step 1**：`server/shared/pkg/rule/` 新增 `action_registry.go`
   ```go
   type RuleActionHandler interface {
       Type() string                    // 动作类型标识，如 "apply_buff"
       Execute(ctx ActionContext) error // 执行动作
   }
   // 全局注册表：map[string]RuleActionHandler
   func RegisterActionHandler(h RuleActionHandler)
   ```
2. **Step 2**：内置7个动作（apply_buff/remove_buff/modify_resource/spawn_entity/trigger_combat/send_notify/change_terrain）各自独立文件 `init()` 注册；规则引擎执行动作改为查表分发。
3. **Step 3**：特殊玩法新动作 = 题材层新文件 `init()` 注册（blank import），**框架零改动**。启动时全量校验"配置引用的 type 均已注册，否则拒绝启动"。

**效果**：新增扩展点从"改框架代码→全量回归"变为"加文件注册→模块回归"。

### 5.2 新增子系统同理

新 module 只依赖 L0/L1 接口 + 事件总线（已有 eventbus），不反向改 L1。回归 = 该模块测试，不再全量。spec.md 已埋 `custom` 逃逸口正好借此落地。

### 5.3 server/ 目录调整建议（最小改动路径）

**当前问题**：12根骨头的机制层散落在 combat/economy/social/operation 各服务 domain 里，换皮时骨头动不了。

**最小改动**（先不动服务拆分）：先把动作/状态机全部注册表化，让题材层可独立开 module。

**完整改动**（行为注册表验证通过后）：
```
server/
  shared/           L0 基础设施（不变）
  framework/        L1 共性机制（新，arena combat/economy/... domain 中搬入）
  config/           L2 配置加载（不变）
  extensions/       L3 题材扩展（新）
    insect/         昆虫世界题材
    sangoku/        三国题材（未来）
    starcraft/      星际题材（未来）
  gateway/          （不变）
  ...其他service    （逐步瘦身，仅保留题材适配+编排层）
```

### 5.4 规则引擎升级

| 现状态 | 目标状态 |
|--------|---------|
| 动作类型写死 enum（7种） | 注册表驱动（启动时校验） |
| 每扩展点单执行器 | 支持多handler链（按优先级分发） |
| `ExtensionRegistry` 纯数据 | 数据注册表（存） + 行为注册表（增） |
| 条件表达式仅 `>/</==/in/and/or` | 加 `random(min,max)` + 种子化确定性随机 |

---

## 六、下一步行动

| 步骤 | 内容 | 负责 |
|------|------|------|
| 1 | 确认本方案 → 立项 | 用户拍板 |
| 2 | P0-1 玩家档案：player 实体契约 + user-auth 接入规格 | 可调度程基岩出 Epic |
| 3 | 行为注册表：`action_registry.go` + 7个内置handler拆分 + 注册表校验 | 可调度程基岩出 ADR + 实现 |
| 4 | P0-2~P0-6 建筑升级/采集调度/背包/兵种/科技 契约设计 | 可调度文策渊出逐系统 GDD |
| 5 | 垂直切片验证：玩家注册→建筑升级→产兵→采集→战斗→结算→背包道具 | 主链路跑通 |

---

## 七、评审与决策记录

| 日期 | 事项 | 结论 |
|------|------|------|
| 2026-08-10 | 框架设计评审 | CONCERNS，方向成立但3个高风险 |
| 2026-08-10 | 目标边界修正 | 通用=SLG共性，特殊=扩展开发 |
| 2026-08-11 | 三项决策确认 | 邮件后置 / 任务配置化 / 聊天留扩展 |
| 2026-08-11 | 本方案终版 | 待用户拍板 |
